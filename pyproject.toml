[project]
name = "olist-ecommerce-platform"
version = "0.1.0"
description = "Olist E-commerce Analytics Platform"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "numpy",
    "sqlalchemy",
    "psycopg2-binary",
    "python-dotenv",
    "pyyaml",
    "openai",
    "chromadb",
    "streamlit",
    "plotly",
    "matplotlib",
    "tabulate",
    "prefect",
    "dbt-postgres",
    "pytest",
]

[tool.uv]
package = false
